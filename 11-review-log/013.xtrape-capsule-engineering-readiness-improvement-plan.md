# xtrape-capsule Engineering Readiness Improvement Plan

Date: 2026-05-06

## Purpose

The public documentation cleanup PRs have been merged. The project has moved from:

```text
public-facing docs cleanup
```

to:

```text
engineering readiness for Public Review
```

The current goal is **not** to continue rewriting product positioning or README copy. The current goal is to make the repositories installable, buildable, testable, and reviewable by external developers.

Target stage:

```text
Public Review before v0.1.0 Public Preview
```

---

# Current Repository Set

```text
xtrape-capsule-site
xtrape-capsule-ce
xtrape-capsule-agent-node
xtrape-capsule-contracts-node
```

Private/internal design repository:

```text
xtrape-capsule-docs
```

`xtrape-capsule-docs` should not be used as the public review entry point.

---

# Current Status Summary

## Public docs status

```text
[OK] site README has Live site link.
[OK] CE README is public-facing.
[OK] Agent SDK README is usable for Public Review.
[OK] Contracts README is usable for Public Review.
[OK] CAPI / capi / capi-chatgpt have been removed from public-facing docs.
[OK] OPENAI_BASE_URL has been replaced with provider-neutral examples.
[OK] HealthStatus / effectiveStatus distinction is documented.
```

## Remaining engineering risks

```text
[Risk] CE fresh install may still fail because @xtrape/capsule-agent-node and @xtrape/capsule-contracts-node are not published to npm.
[Risk] Agent SDK depends on unpublished @xtrape/capsule-contracts-node.
[Risk] CI workflows are not fully merged/validated across repos.
[Risk] Contracts and Agent SDK need stronger test coverage before Public Review.
[Risk] Public Review feedback issue/template and readiness checklist need to be finalized.
```

---

# Overall Execution Strategy

Do not make large feature changes.

Prioritize in this order:

```text
1. Stabilize contracts-node.
2. Stabilize agent-node.
3. Fix CE dependency chain.
4. Add/validate CE CI.
5. Add/validate site docs build CI.
6. Add/validate Public Review issue/template and readiness checklist.
7. Only then start Public Review outreach.
```

---

# P0 - Must Complete Before Public Review

## P0-1. Stabilize `xtrape-capsule-contracts-node`

Repository:

```text
xtrape-capsule-contracts-node
```

Current open PRs:

```text
#2 ci: add package validation workflow
#3 test: add schema validation coverage
```

### Required actions

1. Review and merge CI workflow PR.
2. Review and merge schema validation tests.
3. Confirm package can pass:

```bash
pnpm install
pnpm build
pnpm typecheck
pnpm test
```

4. Confirm package metadata is ready for future npm publication:

```text
package.json:
- name
- version
- description
- license
- main
- module
- types
- files
- publishConfig
- repository
- keywords
```

5. Confirm README examples match actual exported names.

### Suggested tests

Add or confirm coverage for:

```text
RegisterAgentRequestSchema
ServiceReportRequestSchema
HealthReportInputSchema
ConfigItemInputSchema
ActionDefinitionInputSchema
CommandResultSchema
ErrorCode exports
newId prefix helpers
list/pagination helpers
```

### Acceptance criteria

```text
[ ] `pnpm install` passes.
[ ] `pnpm build` passes.
[ ] `pnpm typecheck` passes.
[ ] `pnpm test` passes.
[ ] GitHub Actions CI passes.
[ ] README examples compile or match real exports.
[ ] No provider-specific example such as OPENAI_BASE_URL exists.
```

---

## P0-2. Stabilize `xtrape-capsule-agent-node`

Repository:

```text
xtrape-capsule-agent-node
```

Current open PRs:

```text
#2 ci: add agent validation workflow
#3 test: add token store and agent flow coverage
```

### Main risk

Agent SDK depends on unpublished:

```text
@xtrape/capsule-contracts-node
```

Before CI can be reliable, decide how this dependency is resolved.

### Required dependency decision

Choose one:

## Option A - Publish contracts package first

Publish:

```text
@xtrape/capsule-contracts-node@0.1.0
```

Then Agent SDK can depend on the npm package.

## Option B - Use local/tarball dependency during Public Review

Use a local package or tarball-based dependency in CI until npm publishing is ready.

Examples:

```text
pnpm pack in contracts-node
install generated tarball in agent-node CI
```

Only use this if CI can remain deterministic.

## Option C - Monorepo/workspace approach

Not recommended unless the repositories are reorganized.

### Required actions

1. Review Agent CI PR.
2. Confirm CI handles the unpublished contracts dependency.
3. Review token store and agent flow tests.
4. Confirm package can pass:

```bash
pnpm install
pnpm build
pnpm typecheck
pnpm test
```

5. Confirm README token file format matches implementation:

```text
<agentId>:<agentToken>
```

### Suggested tests

Add or confirm coverage for:

```text
FileTokenStore save/load
FileTokenStore missing file behavior
FileTokenStore clear/delete behavior
registration token missing behavior
stored agent token reuse
new registration flow
health provider reporting
config provider reporting
action prepare behavior
action execute behavior
command polling and result reporting
redaction / no token logging
```

### Acceptance criteria

```text
[ ] contracts dependency is resolved in a deterministic way.
[ ] `pnpm install` passes.
[ ] `pnpm build` passes.
[ ] `pnpm typecheck` passes.
[ ] `pnpm test` passes.
[ ] GitHub Actions CI passes.
[ ] README token storage description matches implementation.
[ ] README package status remains honest if npm package is not yet published.
```

---

## P0-3. Fix `xtrape-capsule-ce` fresh install and validation

Repository:

```text
xtrape-capsule-ce
```

Current open PRs:

```text
#3 build: use workspace packages for CE validation
#4 ci: add CE validation workflow
#5 docs: add public review issue template
#6 docs: add public review readiness checklist
```

### Main risk

CE currently depends on packages that may not yet be published:

```text
@xtrape/capsule-agent-node
@xtrape/capsule-contracts-node
```

This can block:

```bash
pnpm install
pnpm typecheck
pnpm build
```

from a fresh clone.

### Required actions

1. Review and merge PR #3 first.
2. Ensure CE can validate without requiring unpublished npm packages.
3. Then review and merge PR #4.
4. Confirm a fresh clone can run:

```bash
pnpm install
pnpm contracts:check
pnpm db:validate
pnpm typecheck
pnpm build
```

5. If tests exist:

```bash
pnpm test
```

### Dependency strategy

Preferred for Public Review:

```text
Use workspace/local packages for validation, or publish the required packages before CE depends on them.
```

Do not leave CE in a state where a public user runs `pnpm install` and receives registry 404.

### Acceptance criteria

```text
[ ] Fresh clone install works.
[ ] CE does not fail because unpublished packages are missing from npm.
[ ] `pnpm contracts:check` passes.
[ ] `pnpm db:validate` passes.
[ ] `pnpm typecheck` passes.
[ ] `pnpm build` passes.
[ ] GitHub Actions CI passes.
[ ] README Quick Start remains accurate.
```

---

## P0-4. Add and validate `xtrape-capsule-site` docs CI

Repository:

```text
xtrape-capsule-site
```

Current open PR:

```text
#2 ci: add docs build workflow
```

### Required actions

1. Review and merge docs build CI.
2. Confirm workflow runs on pull requests.
3. Confirm deploy workflow only publishes from main.
4. Confirm this command passes in CI:

```bash
pnpm install
pnpm docs:build
```

### Acceptance criteria

```text
[ ] PR docs build workflow passes.
[ ] GitHub Pages deploy workflow remains main-only.
[ ] Live site still works.
[ ] README badge points to correct workflow if badges are used.
```

---

# P1 - Should Complete Before Wider Public Review

## P1-1. Public Review feedback entry

Repository:

```text
xtrape-capsule-ce
```

Current open PR:

```text
#5 docs: add public review issue template
```

### Required actions

1. Add a Public Review issue template or create a pinned issue.
2. The feedback entry should ask reviewers:

```text
1. Can you understand what Xtrape Capsule is within 30 seconds?
2. Does the Capsule Service concept make sense?
3. Can you follow the Quick Start?
4. Is Agent registration clear?
5. Is the security boundary acceptable?
6. What would block you from trying it?
7. Which use case should be prioritized?
```

3. Link to:

```text
xtrape-capsule-site
xtrape-capsule-ce
xtrape-capsule-agent-node
xtrape-capsule-contracts-node
```

4. Clearly state:

```text
This is Public Review, not production-ready.
```

### Acceptance criteria

```text
[ ] Public Review issue template exists, or a pinned issue exists.
[ ] Feedback questions are concrete.
[ ] Public site and repo links are included.
[ ] Not production-ready status is clear.
```

---

## P1-2. Public Review readiness checklist

Repository:

```text
xtrape-capsule-ce
```

Current open PR:

```text
#6 docs: add public review readiness checklist
```

### Required checklist content

Create or confirm:

```text
docs/public-review-readiness.md
```

Include:

```text
[ ] site docs:build passed
[ ] contracts build/typecheck/test passed
[ ] agent build/typecheck/test passed
[ ] CE fresh install passed
[ ] CE contracts:check passed
[ ] CE db:validate passed
[ ] CE typecheck/build passed
[ ] no CAPI/capi/capi-chatgpt in public docs
[ ] no OPENAI_BASE_URL in public examples
[ ] no placeholder text such as ****
[ ] Public Review issue exists
[ ] Live site accessible
[ ] xtrape-capsule-docs is private
```

### Acceptance criteria

```text
[ ] Checklist exists.
[ ] Checklist is linked from README or release/prep docs.
[ ] Checklist reflects current Public Review status.
```

---

## P1-3. Add minimal release/readiness script

Repository:

```text
xtrape-capsule-ce
```

CE already has scripts such as:

```text
repo:check
release:check
contracts:check
db:validate
```

### Required action

Confirm or add one command that maintainers can run before Public Review:

```bash
pnpm release:check
```

It should include:

```text
contracts:check
db:validate
typecheck
build
```

If tests exist, include tests.

### Acceptance criteria

```text
[ ] One clear command exists for maintainers.
[ ] README or readiness doc references it.
[ ] Command passes after dependency fix.
```

---

# P2 - Good Follow-up Tasks

## P2-1. Prepare npm publication path

Repositories:

```text
xtrape-capsule-contracts-node
xtrape-capsule-agent-node
```

### Required actions

1. Decide package version:

```text
0.1.0
```

or:

```text
0.1.0-public-review.0
```

2. Confirm package metadata.
3. Confirm `pnpm pack` output.
4. Confirm package contents include only needed files:

```text
dist/
README.md
LICENSE
NOTICE
SECURITY.md if desired
```

5. Add publish checklist:

```text
docs/publishing.md
```

### Acceptance criteria

```text
[ ] `pnpm pack` works.
[ ] package contents are correct.
[ ] npm publish process is documented.
```

---

## P2-2. Prepare GHCR image publication path

Repository:

```text
xtrape-capsule-ce
```

### Required actions

1. Confirm Dockerfile and compose path.
2. Add GHCR build workflow later.
3. Document current status clearly:

```text
Public Docker images are planned but not yet required for Public Review.
```

### Acceptance criteria

```text
[ ] Current Quick Start remains source build.
[ ] No docs instruct users to pull an unavailable image.
[ ] GHCR publication is tracked as future issue.
```

---

## P2-3. Add screenshots and demo GIF

Repositories:

```text
xtrape-capsule-site
xtrape-capsule-ce
```

### Suggested assets

```text
screenshots/dashboard.png
screenshots/agents.png
screenshots/services.png
screenshots/service-detail.png
screenshots/action-execution.png
screenshots/audit-events.png
demo/opstage-demo.gif
```

### Acceptance criteria

```text
[ ] No secrets in screenshots.
[ ] At least one real UI screenshot before wider promotion.
[ ] Demo GIF before Product Hunt / Hacker News style launch.
```

---

# Recommended Merge / Review Order

Follow dependency order.

```text
1. contracts-node #2 CI
2. contracts-node #3 schema tests
3. agent-node #2 CI
4. agent-node #3 token store and agent flow tests
5. CE #3 workspace dependency validation
6. CE #4 CI workflow
7. site #2 docs build CI
8. CE #5 Public Review issue template
9. CE #6 readiness checklist
```

If CI in Agent or CE depends on unpublished packages, resolve dependency first.

---

# Validation Commands

Run these where applicable.

## Shared search checks

```bash
rg -n "CAPI|capi|capi-chatgpt|OPENAI_BASE_URL" .
```

Expected:

```text
No public-facing matches.
```

```bash
rg -n "\\*\\*\\*\\*|Live site:\\s*$|as `:`|Site:\\s*$|Contracts overview:\\s*$" .
```

Expected:

```text
No unresolved placeholders.
```

## Site

```bash
pnpm install
pnpm docs:build
```

## Contracts

```bash
pnpm install
pnpm build
pnpm typecheck
pnpm test
```

## Agent

```bash
pnpm install
pnpm build
pnpm typecheck
pnpm test
```

## CE

```bash
pnpm install
pnpm contracts:check
pnpm db:validate
pnpm typecheck
pnpm build
pnpm test
```

Use actual scripts if names differ.

---

# Public Review Start Criteria

Do not start public outreach until:

```text
[ ] contracts-node CI passes.
[ ] contracts-node schema tests pass.
[ ] agent-node CI passes.
[ ] agent-node token/flow tests pass.
[ ] CE fresh install works.
[ ] CE CI passes.
[ ] site docs build CI passes.
[ ] Public Review feedback issue/template exists.
[ ] Public Review readiness checklist exists.
[ ] Live site is accessible.
[ ] xtrape-capsule-docs is private or removed from public entry path.
```

---

# AI Task Instruction

Use this instruction when assigning work to AI:

```text
Current goal: engineering readiness for Xtrape Capsule Public Review.

Do not rewrite positioning or make large product changes.

Please work repo by repo and PR by PR.

Priority:
1. Contracts CI and schema tests.
2. Agent CI and token/agent flow tests.
3. CE dependency chain and CI.
4. Site docs build CI.
5. Public Review feedback issue/template.
6. Public Review readiness checklist.

For each PR, include:
- files changed;
- validation commands run;
- pass/fail results;
- if failed, exact reason;
- whether unpublished npm package dependency is involved.

Do not merge unrelated changes into one PR.
```

---

# Final Goal

After this plan is complete, the project should be ready for a controlled:

```text
Public Review Call
```

not a full launch.

The goal of Public Review is to validate:

```text
clarity
runnability
trust
developer experience
use-case fit
```

Do not optimize for stars yet.
